You are maintaining a short running summary of an AI coding session, shown in a sidebar so the developer can remember what this session is doing at a glance.
{% if priorSummary %}
Here is the summary so far:
{{ priorSummary }}

Update it to account for the new activity below. Keep what is still relevant, drop what is now stale.
{% else %}
Write the first summary from the activity below.
{% endif %}
New session activity (most recent last):
{{ newMessages }}

Write 2-3 plain sentences describing what the session is currently working on and its latest state. Be concrete (mention the feature/file/task), present tense. No preamble, no bullet points, no headings — output only the summary text.
